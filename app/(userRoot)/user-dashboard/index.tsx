import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const UserHome = () => {
  return (
    <SafeAreaView className="flex-1 items-center justify-center">
      <Text>User Side Bar</Text>
    </SafeAreaView>
  );
};

export default UserHome;
